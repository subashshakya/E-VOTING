import 'package:flutter/material.dart';

class SuccessVoting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Voting process is over"));
  }
}
